# Only needed for local development — in production the Cloudflare Worker
# injects the API key server-side.
VITE_OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Optional: override the AI endpoint (defaults to the Cloudflare Worker)
# VITE_AI_ENDPOINT=https://openrouter.ai/api/v1/chat/completions
